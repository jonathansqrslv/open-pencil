[build]
builder = "nixpacks"
buildCommand = "bun install --frozen-lockfile && bun run build:packages && bun run build"

[deploy]
startCommand = "bun run preview"
healthcheckPath = "/"
healthcheckTimeout = 300
